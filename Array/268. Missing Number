class Solution {
    public int missingNumber(int[] nums) {
        int sum=0;
        for(int x:nums){
            sum+=x;
        }
        int n=nums.length;
        int totalSum=n*(n+1)/2;
        return totalSum-sum;
    }
}
